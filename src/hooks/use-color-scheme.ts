// Native platforms: use React Native's built-in color-scheme hook directly.
// (The web build uses use-color-scheme.web.ts instead, which handles hydration.)
export { useColorScheme } from 'react-native';
